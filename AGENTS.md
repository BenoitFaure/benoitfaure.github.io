# AGENTS.md

- This checkout currently has no project files besides `.git/`; `git status` shows `index.html`, `resources/css/index.css`, `resources/files/FAURE_B_EN.pdf`, and `resources/js/main.js` deleted from the working tree.
- Do not infer build, test, lint, or deploy commands from the empty worktree. Restore or inspect the tracked files/history first if project guidance is needed.
- Avoid reverting the deleted files unless the user explicitly asks; they may be intentional worktree changes.
